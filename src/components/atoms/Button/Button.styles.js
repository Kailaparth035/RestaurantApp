import { Button, withStyles } from "@ui-kitten/components";
import styled from "styled-components/native";

const StyledButton = styled(Button).attrs((props) => {
  ({});
})``;

export default withStyles(StyledButton, (theme) => ({}));
